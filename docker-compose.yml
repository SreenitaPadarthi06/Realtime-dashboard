services:
  backend:
    build: ./backend
    ports:
      - "5001:5001"
    restart: unless-stopped

  frontend:
    build: .
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped
